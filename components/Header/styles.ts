const header = {
  position: 'absolute',
  top: 0,
  display: 'flex',
  alignItems: 'center',
  width: '100%',
  height: '100px',
};

const headerInner = {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
};

const classes = { header, headerInner };

export default classes;
